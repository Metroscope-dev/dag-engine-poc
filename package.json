{
  "name": "ts-training",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "watch": "tsc -w",
    "start": "tsc && node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma-migrate": "npx prisma migrate dev --name init",
    "postgres-start": "docker-compose up",
    "postgres-stop": "docker-compose down"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "tslint": "^6.1.3",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "express": "^4.17.1",
    "prisma": "^3.3.0"
  }
}
